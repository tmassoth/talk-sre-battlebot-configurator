# Port the payment API listens on and the allowed CORS origin (Vite dev server).
PORT=3002
CORS_ORIGIN=http://localhost:5173

# Downstream payment provider. Deliberately unreachable for the demo.
PAYMENT_URL=https://doesnotexist.xxxxxxx
PAYMENT_TIMEOUT=5
